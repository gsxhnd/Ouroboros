# 02 - 架构设计

## 整体架构（HTTP Server）

```
┌─────────────────────────────────────────────────────────┐
│                     Frontend (React)                      │
│     shadcn/ui + zustand + i18next + react-router        │
│    通过浏览器访问，REST API + WebSocket 与 Backend 通信  │
│    web/dist 外置打包，可替换为第三方 UI                    │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP REST / 静态资源
┌────────────────────────▼────────────────────────────────┐
│                    ourboros (Axum)                        │
│   CLI: server                                             │
│   REST API + 可选静态 UI 托管 + WebSocket（规划中）         │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    ourboros-core (Rust lib)               │
│  资源索引 │ 文件监听 │ EXIF解析 │ 缩略图生成 │ 搜索引擎   │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│              Storage Layer (SeaORM)                       │
│   SQLite (本地/默认) │ PostgreSQL │ MySQL (通过配置切换)   │
│   文件系统 (原始文件 + .ourboros/ 元数据目录)             │
└─────────────────────────────────────────────────────────┘
```

## 单一二进制 + 子命令

`ourboros` crate 提供**一个可执行文件**，通过 clap 子命令区分运行模式：

| 子命令 | 用途 | 典型场景 |
|--------|------|----------|
| `ourboros server` | HTTP 服务 | NAS、Docker、systemd、桌面本地运行 |
| `ourboros version` | 打印版本信息 | 运维检查 |

```bash
# 服务端 / 本地桌面
ourboros server --host 0.0.0.0 --port 8080 --web-dir ./web/dist
ourboros server --web-dir /path/to/custom-ui
```

用户通过系统浏览器访问 Web UI，不使用 Tauri / WebView。

## Web UI 托管策略

前端构建产物 `web/dist` **不 embed 进二进制**，运行时从磁盘读取，便于替换第三方 UI：

| 优先级 | 来源 |
|--------|------|
| 1 | CLI `--web-dir` |
| 2 | 环境变量 `OURBOROS_WEB_DIR` |
| 3 | 自动探测：可执行文件旁 `web/`、macOS `../Resources/web` 等 |

未指定或找不到有效目录（需含 `index.html`）时，进入 **API-only** 模式，仅提供 REST API。

Axum 路由：`/api/*`、`/health` 走 API；其余路径 fallback 到静态文件（SPA → `index.html`）。

**开发模式**仍使用 Vite dev server（`:1420`）+ proxy 到后端，无需每次 build dist。

## 部署模式

| 模式 | 命令 | 说明 |
|------|------|------|
| **桌面端** | `ourboros server` | 本地监听（默认 `127.0.0.1:8080`），配合 `--web-dir` 托管 UI。macOS 可打包为 `.app` bundle。 |
| **服务端** | `ourboros server` | 监听固定端口，适合 NAS / 团队共享。可配合 `--web-dir` 托管官方或第三方 UI。 |
| **纯 API** | `ourboros server`（无 `--web-dir`） | 仅 REST API；静态文件由 Nginx 等外部服务托管。 |

## 平台适配

| 平台 | 适配方案 |
|------|----------|
| **macOS** | `.app` bundle + 启动脚本执行 `ourboros server --web-dir ...` |
| **Windows** | 直接运行 `ourboros server`，或通过打包脚本生成便携目录 |
| **Linux** | 通过 `.desktop` 文件启动 `ourboros server` |

## Monorepo 目录结构

```
Ourboros/
├── crates/
│   ├── ourboros-entity/        # SeaORM Entity 定义 (lib crate)
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── asset.rs
│   │       ├── folder.rs
│   │       ├── tag.rs
│   │       ├── asset_folder.rs
│   │       ├── asset_tag.rs
│   │       └── smart_folder.rs
│   │
│   ├── ourboros-migration/     # 数据库迁移 (lib crate)
│   │   └── src/
│   │       ├── lib.rs
│   │       └── m20240101_000001_initial_schema.rs
│   │
│   ├── ourboros-core/          # 核心库 (lib crate)
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── library/        # 资源库管理（创建/打开/切换）
│   │       ├── storage/        # 数据库连接层 (SeaORM)
│   │       ├── config/         # 配置管理
│   │       └── error.rs        # 错误类型
│   │
│   └── ourboros/               # 应用 crate（lib + 单一 binary）
│       └── src/
│           ├── main.rs         # CLI 入口，分发子命令
│           ├── lib.rs          # HTTP 服务库（bind / run / build_router）
│           ├── cli/
│           │   ├── mod.rs      # clap 子命令定义
│           │   └── server.rs   # server 子命令
│           ├── http/           # Axum REST routes
│           ├── state.rs        # AppState (RwLock<LibraryManager>)
│           └── web.rs          # 静态 UI 目录解析与托管
│
├── web/                        # 前端 (React)
│   ├── dist/                   # 构建产物（发布时打包，不 embed）
│   └── src/
│       ├── main.tsx
│       ├── components/         # UI 组件
│       ├── pages/              # 页面（home / library / system / settings）
│       ├── stores/             # zustand stores
│       ├── services/           # API 调用层
│       ├── i18n/               # 多语言
│       └── types/              # TypeScript 类型定义
│
├── scripts/
│   └── bundle-macos.sh         # macOS .app 打包（仅 release 产物，不编译）
│
├── Cargo.toml                  # Workspace Cargo.toml
└── docs/
    └── dev/                    # 开发文档
```
